{
  "name": "pdf-proofreader-vscode",
  "displayName": "Placekeeper",
  "description": "Open one local PDF in the installed Placekeeper service.",
  "version": "0.1.0",
  "private": true,
  "engines": { "vscode": ">=1.95.0" },
  "extensionKind": ["ui"],
  "main": "./dist/extension.js",
  "activationEvents": ["onCommand:pdfProofreader.open"],
  "contributes": {
    "commands": [
      { "command": "pdfProofreader.open", "title": "Placekeeper: Open Local PDF" }
    ],
    "menus": {
      "explorer/context": [
        { "command": "pdfProofreader.open", "when": "resourceExtname == .pdf", "group": "navigation" }
      ],
      "editor/title": [
        { "command": "pdfProofreader.open", "when": "resourceExtname == .pdf", "group": "navigation" }
      ]
    },
    "configuration": {
      "title": "Placekeeper",
      "properties": {
        "pdfProofreader.launcherPath": {
          "type": "string",
          "description": "Absolute path to the installed Placekeeper launcher."
        }
      }
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json"
  }
}
