{
  "name": "placekeeper-vscode",
  "displayName": "Placekeeper",
  "description": "Open one local PDF in the installed Placekeeper service.",
  "version": "0.1.0",
  "private": true,
  "engines": { "vscode": ">=1.95.0" },
  "extensionKind": ["ui"],
  "main": "./dist/extension.js",
  "activationEvents": ["onCommand:placekeeper.open"],
  "contributes": {
    "commands": [
      { "command": "placekeeper.open", "title": "Placekeeper: Open Local PDF" }
    ],
    "menus": {
      "explorer/context": [
        { "command": "placekeeper.open", "when": "resourceExtname == .pdf", "group": "navigation" }
      ],
      "editor/title": [
        { "command": "placekeeper.open", "when": "resourceExtname == .pdf", "group": "navigation" }
      ]
    },
    "configuration": {
      "title": "Placekeeper",
      "properties": {
        "placekeeper.launcherPath": {
          "type": "string",
          "description": "Absolute path to the installed Placekeeper launcher."
        }
      }
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json"
  }
}
