# App icon proposals

Open [index.html](index.html) to compare three candidates. The app-icon section of the [design contract](../../../2026-09-05-neutral-soft-design-contract.md) records scope, decisions, and production acceptance requirements.

The user selected a muted blue path and small amber dot. All candidates preserve the two-page reference detour and return metaphor from the existing master. A retains outlines, B uses soft borderless layers and shallow depth, and C reduces details and strengthens the path. The user selected B — Soft pages, including its borderless layers and subtle depth. Final production artwork and small-size optical refinement remain implementation work.

- `current.svg` is a comparison copy of the current production master.
- `a-familiar.svg`, `b-soft-pages.svg`, and `c-simple-mark.svg` are editable candidate artwork, not production assets.
- `options.html` is the conversation comparison fragment with embedded copies of the artwork.
- `index.html` is generated with the installed Visualize renderer from `options.html`.

The comparison shows a rounded display crop and 48, 32, and 16 px samples. These are scaled vector previews, not hand-adjusted small-size masters or verified native Dock screenshots. Final iconset work must address small-size legibility and validate the real packaged app. The backdrop can be changed through the conversation's design controls; this does not propose a dark interface theme.

If changing a candidate, update its embedded copy in `options.html` and regenerate `index.html`. Keep the conversation copy synchronized. No production master, iconset PNG, bundle identifier, or installer is changed by this proposal.

## Selected B refinement

Open [the before/after comparison](refinement-review.html). The reference page has a modestly wider fan than the original B while remaining closely tucked. The amber dot sits near the middle of the exposed portion, and the whole stack is centered with balanced outside margins. The earlier broadly exposed treatment was rejected. `b-soft-pages.svg` contains this refinement; `b-soft-pages-before.svg` preserves the earlier selected treatment for comparison. `refinement.html` is its embedded-artwork fragment and `refinement-review.html` is the rendered standalone review. The original three-option comparison also shows the refined B.

The latest refinement raises the back sheet slightly and extends its diagonal lower edge behind the front sheet. Its upper corner stays concealed, and the lower corner does not interrupt the exposed diagonal. The selected fan and page-stack position are retained.
